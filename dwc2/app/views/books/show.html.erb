<table>
<tbody>
    <tr>
      <th>image</th>
      <td><%= link_to @book.title, "/books/#{@book.id}", class: "show_#{@book.id}" %></td>
      <td><%= @book.opinion %></td>
      <td><%= link_to 'Edit', edit_book_path(@book), class: "edit_#{@book.id}" %></td>
      <td><%= link_to 'Destroy', @book, method: :delete, data: { confirm: 'Are you sure?'}, class: "destroy_#{@book.id}"  %></td>
    </tr>
  </tbody>
</table>

<% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
    
<br>
<%= link_to 'Edit', edit_book_path(@book.id), class: "edit_#{@book.id}" %>｜<%= link_to 'Back', "/books", class: "back" %>